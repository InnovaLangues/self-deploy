---
  - hosts: web
    sudo: yes
    gather_facts: false
    vars_files:
      - preprod.yml
    roles:
      - pre_deploy

  - hosts: web
    sudo: yes
    gather_facts: false
    vars_files:
      - preprod.yml
    roles:
      - webtier

  - hosts: web
    sudo: yes
    gather_facts: false
    vars_files:
      - preprod.yml
    roles:
      - post_deploy